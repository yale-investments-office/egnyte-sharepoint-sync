# Environment Variables for Local Development

# Azure Key Vault Configuration (for production)
KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

# Egnyte Configuration
EGNYTE_DOMAIN=yaleinvestments.egnyte.com
EGNYTE_REDIRECT_URI=http://localhost:3002

# For local development only (use Key Vault in production)
EGNYTE_CLIENT_ID=your-egnyte-client-id
EGNYTE_CLIENT_SECRET=your-egnyte-client-secret

# SharePoint/Microsoft Graph Configuration  
AZURE_REDIRECT_URI=http://localhost:3002

# For local development only (use Key Vault in production)
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# SharePoint Target Configuration (Optional - for pre-configured sync)
# Leave these empty for dynamic site selection, or fill them in for a default target
SHAREPOINT_SITE_URL=
SHAREPOINT_SITE_ID=
SHAREPOINT_LIBRARY_NAME=
SHAREPOINT_LIBRARY_ID=

# Development URLs
FRONTEND_URL=http://localhost:3002
BACKEND_URL=http://localhost:7071
